<%= simple_form_for([:admin,@answer]) do |f| %>
  <%= render 'shared/error_messages', target: @answer %>
  <fieldset>
    <legend></legend>
    <%= f.input :body, as: :text, input_html: { class: "span8", rows: "10"} %>
    <%= f.input :user_id %>
    <div class="form-actions">
      <%= f.submit t("common.save"), class: "btn btn-primary", 'data-disable-with' => t("common.saving") %>
      or
      <%= link_to 'Back', admin_answers_path %>
    </div>
  </fieldset>
<% end %>
